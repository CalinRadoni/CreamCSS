#!/bin/ash

# initialize the package.json with default values
#npm init -y

# add dependencies
npm install --save-dev gulp \
    gulp-less \
    gulp-clean-css \
    gulp-concat \
    gulp-rename \
    gulp-replace \
    del
